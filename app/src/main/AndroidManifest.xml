<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.listecourse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".accueil">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".magasins"
            android:label="@string/title_activity_magasins"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ajoutmagasin"
            android:label="@string/title_activity_ajoutmagasin"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name="listeproduit"
            android:label="listeprod"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name=".affichageproduit"
            android:label="affichageprod"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>